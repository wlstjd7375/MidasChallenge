<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kimjs.sample" >

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.LoadingActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.MainActivity"
            android:label="@string/title_activity_main" >
        </activity>

        <activity
            android:name=".activity.BusSettingActivity"
            android:label="@string/title_activity_bus" >
        </activity>

        <activity android:name=".activity.SubwaySettingActivity"
            android:label="@string/title_activity_subway" >
        </activity>

        <activity android:name=".activity.WeatherSettingActivity"
            android:label="@string/title_activity_weather" >
        </activity>

    </application>

</manifest>
